services:
  registry:
    image: registry:2
    container_name: k3s-registry
    restart: unless-stopped
    ports:
      - "0.0.0.0:5000:5000"
    volumes:
      - registry-data:/var/lib/registry

volumes:
  registry-data:
